// Define a JavaScript function called logMsg() that can be used to log an error message for any object that contains the property errMsg. 
const logger = {
    logMsg: function () {
        if()
    }
}

const obj1 = {
    errm
}

function logMsg() {
    console.log(`Error Massege: ${this.errMsg}`);
}

console.log(logMsg())